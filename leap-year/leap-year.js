// exports.isLeapYear = function(year) {
//   return ((year % 4 === 0) && (year % 100 !== 0)) || (year % 400 === 0);
// };
//
//
// //test two
// // exports.isLeapYear = function(year) {
// //   return year % 4 === 0 && year % 100 !== 0;  // second attempt
// // };
//
// //test one
// // exports.isLeapYear = function(year) {
// //   return year % 4 === 0;                // our first attempt at getting the logic correct.
// // };
